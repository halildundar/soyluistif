{{> header}}
<div class="max-w-[--max-w] mx-auto">
    <div class="py-2 text-gray-500 font-semibold  flex items-center space-x-2 text-[0.8rem] px-2">

        <div>
            {{respData.status}}
        </div>
        <div>{{respData.msg}}</div>
    </div>
</div>
{{> footer}}